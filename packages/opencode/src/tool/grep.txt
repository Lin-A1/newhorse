- 快速的内容搜索工具，适用于任何规模的代码库
- 使用正则表达式搜索文件内容
- 支持完整的正则语法（如 "log.*Error"、"function\s+\w+" 等）
- 通过 include 参数按模式过滤文件（如 "*.js"、"*.{ts,tsx}"）
- 返回包含匹配行的文件路径和行号
- 当你需要查找包含特定模式的文件时，使用此工具
- 如果你需要识别/统计文件中的匹配数量，请直接用 Bash 工具执行 `rg`（ripgrep）。不要使用 `grep`。
- 当你在进行可能需要多轮 glob 和 grep 的开放式搜索时，改用 Task 工具
