获取文件符号（大纲）或搜索工作区符号。

参数：
- filePath: 用于选择并启动匹配文件类型的 LSP 服务器
- scope: "document"（默认）获取文件内的符号大纲；"workspace" 在项目中搜索符号
- query: 符号搜索关键字（workspace 范围必填；空字符串表示请求所有符号）
- limit: 最大返回结果数（默认 50）

注意：必须为文件类型配置对应的 LSP 服务器。如果没有可用服务器，将返回错误。
