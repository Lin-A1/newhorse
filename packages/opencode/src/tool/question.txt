当你在执行过程中需要向用户提问时，使用此工具。这让你可以：
1. 收集用户的偏好或需求
2. 澄清含糊的指令
3. 在工作中就实现上的选择做出决策
4. 向用户提供关于接下来方向的选项。

使用说明：
- 当 `custom` 开启（默认开启）时，会自动添加一个 "Type your own answer"（输入你自己的答案）选项；不要包含 "Other" 或兜底选项
- 答案以标签数组的形式返回；设置 `multiple: true` 以允许多选
- 如果你推荐某个特定选项，把它放在列表的第一个位置，并在标签末尾加上 "(Recommended)"
