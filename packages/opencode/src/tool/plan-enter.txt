当任务复杂、跨多模块、或需要先规划再实施时，使用此工具进入 plan（规划）模式。

此工具会询问用户确认，然后切换到 plan agent 开始规划——先只读探索、问清问题、
写出计划文件，最后通过 plan_exit 返回 build 实施。

在以下情况调用此工具：
- 任务规模大或涉及多个模块，直接动手容易走偏
- 需求模糊，需要先调研代码再确认方案
- 用户明确要求"先规划"或"先出方案"

不要调用此工具：
- 对简单、范围明确的小改动
- 如果用户已经明确要求直接开始实施
