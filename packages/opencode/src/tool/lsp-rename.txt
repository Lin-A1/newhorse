在工作区范围内重命名符号，并应用语言服务器返回的改动（会修改所有受影响的文件）。

参数：
- filePath: 要操作的文件
- line: 行号（从 1 开始，与编辑器中显示一致）
- character: 字符偏移（从 0 开始，与语言服务器约定一致）
- newName: 符号的新名称

建议先用 lsp_prepare_rename 确认该位置支持重命名。注意：必须为文件类型配置对应的 LSP 服务器。
