---
title: GitHub
description: Koristite Newhorse u GitHub problemima i zahtjevima za povlačenjem.
---

Newhorse se integriše sa vašim GitHub tokovom rada. Spomenite `/newhorse` ili `/oc` u svom komentaru i Newhorse će izvršiti zadatke unutar vašeg GitHub Actions runnera.

---

## Funkcije

- **Problemi trijaže**: Zamolite Newhorse da ispita problem i objasni vam ga.
- **Popravi i implementiraj**: Zamolite Newhorse da popravi problem ili implementira funkciju. I radit će u novoj poslovnici i dostavljati PR sa svim promjenama.
- **Secure**: Newhorse se pokreće unutar pokretača vašeg GitHub-a.

---

## Instalacija

Pokrenite sljedeću naredbu u projektu koji se nalazi u GitHub repo:

```bash
newhorse github install
```

Ovo će vas provesti kroz instalaciju GitHub aplikacije, kreiranje toka posla i postavljanje tajni.

---

### Ručno podešavanje

Ili ga možete postaviti ručno.

1. **Instalirajte GitHub aplikaciju**
   Idite na [**github.com/apps/newhorsegent**](https://github.com/apps/newhorsegent). Uvjerite se da je instaliran na ciljnom spremištu.
2. **Dodajte radni tok**
   Dodajte sljedeći fajl toka posla u `.github/workflows/newhorse.yml` u svoj repo. Obavezno postavite odgovarajuće `model` i potrebne API ključeve u `env`.

```yml title=".github/workflows/newhorse.yml" {24,26}
   name: newhorse

   on:
     issue_comment:
       types: [created]
     pull_request_review_comment:
       types: [created]

   jobs:
     newhorse:
       if: |
         contains(github.event.comment.body, '/oc') ||
         contains(github.event.comment.body, '/newhorse')
       runs-on: ubuntu-latest
       permissions:
         id-token: write
       steps:
          - name: Checkout repository
            uses: actions/checkout@v6
            with:
              fetch-depth: 1
              persist-credentials: false

          - name: Run Newhorse
           uses: Lin-A1/newhorse/github@latest
           env:
             ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
           with:
             model: anthropic/claude-sonnet-4-20250514
             # share: true
             # github_token: xxxx
```

3. **Sačuvaj API ključeve u tajne**
   U **postavkama** organizacije ili projekta proširite **Tajne i varijable** na lijevoj strani i odaberite **Radnje**. I dodajte potrebne API ključeve.

---

## Konfiguracija

- `model`: Model za korištenje s Newhorse. Uzima format `provider/model`. Ovo je **obavezno**.
- `agent`: Agent za korištenje. Mora biti primarni agent. Vraća se na `default_agent` iz konfiguracije ili `"build"` ako nije pronađen.
- `share`: Da li dijeliti Newhorse sesiju. Podrazumevano je **true** za javna spremišta.
- `prompt`: Opcioni prilagođeni upit za nadjačavanje zadanog ponašanja. Koristite ovo da prilagodite kako Newhorse obrađuje zahtjeve.
- `token`: Opcionalni GitHub pristupni token za izvođenje operacija kao što su kreiranje komentara, upisivanje promjena i otvaranje zahtjeva za povlačenjem. Newhorse prema zadanim postavkama koristi token za pristup instalaciji iz aplikacije Newhorse GitHub, tako da se urezivanje, komentari i zahtjevi za povlačenjem pojavljuju kao da dolaze iz aplikacije.
  Alternativno, možete koristiti GitHub Action runner [ugrađeni `GITHUB_TOKEN`](https://docs.github.com/en/actions/tutorials/authenticate-with-github_token) bez instaliranja Newhorse GitHub aplikacije. Samo se pobrinite da date potrebna odobrenja u svom toku rada:

```yaml
permissions:
  id-token: write
  contents: write
  pull-requests: write
  issues: write
```

Također možete koristiti [Personal Access Tokens](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens)(PAT) ako želite.

---

## Podržani događaji

Newhorse se može pokrenuti sljedećim GitHub događajima:
| Vrsta događaja | Pokrenuo | Detalji
|----------------------------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `issue_comment` | Komentirajte problem ili PR | Navedite `/newhorse` ili `/oc` u svom komentaru. Newhorse čita kontekst i može kreirati grane, otvarati PR-ove ili odgovarati. |
| `pull_request_review_comment` | Komentirajte određene linije koda u PR-u | Navedite `/newhorse` ili `/oc` dok pregledavate kod. Newhorse prima putanju datoteke, brojeve redova i kontekst razlike. |
| `issues` | Broj otvoren ili uređen | Automatski pokrenite Newhorse kada se problemi kreiraju ili modificiraju. Zahtijeva `prompt` unos. |
| `pull_request` | PR otvoren ili ažuriran | Automatski pokrenite Newhorse kada se PR-ovi otvore, sinkroniziraju ili ponovo otvore. Korisno za automatske recenzije. |
| `schedule` | Cron baziran raspored | Pokrenite Newhorse prema rasporedu. Zahtijeva `prompt` unos. Izlaz ide u dnevnike i PR-ove (nema problema za komentarisanje). |
| `workflow_dispatch` | Ručni okidač iz GitHub korisničkog sučelja | Aktivirajte Newhorse na zahtjev preko kartice Akcije. Zahtijeva `prompt` unos. Izlaz ide u dnevnike i PR-ove. |

### Primjer rasporeda

Pokrenite Newhorse po rasporedu za obavljanje automatiziranih zadataka:

```yaml title=".github/workflows/newhorse-scheduled.yml"
name: Scheduled Newhorse Task

on:
  schedule:
    - cron: "0 9 * * 1" # Every Monday at 9am UTC

jobs:
  newhorse:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Run Newhorse
        uses: Lin-A1/newhorse/github@latest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        with:
          model: anthropic/claude-sonnet-4-20250514
          prompt: |
            Review the codebase for any TODO comments and create a summary.
            If you find issues worth addressing, open an issue to track them.
```

Za zakazane događaje, unos `prompt` je **potreban** jer nema komentara za izvlačenje instrukcija. Planirani tokovi posla se pokreću bez korisničkog konteksta za provjeru dozvola, tako da tok posla mora odobriti `contents: write` i `pull-requests: write` ako očekujete da će Newhorse kreirati grane ili PR-ove.

---

### Primjer zahtjeva za povlačenjem

Automatski pregledajte PR-ove kada se otvore ili ažuriraju:

```yaml title=".github/workflows/newhorse-review.yml"
name: newhorse-review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: read
      issues: read
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: Lin-A1/newhorse/github@latest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          model: anthropic/claude-sonnet-4-20250514
          use_github_token: true
          prompt: |
            Review this pull request:
            - Check for code quality issues
            - Look for potential bugs
            - Suggest improvements
```

Za `pull_request` događaje, ako nije naveden `prompt`, Newhorse podrazumevano pregledava zahtjev za povlačenjem.

---

### Primjer trijaže problema

Automatski triažirajte nove probleme. Ovaj primjer filtrira na račune starije od 30 dana radi smanjenja neželjene pošte:

```yaml title=".github/workflows/newhorse-triage.yml"
name: Issue Triage

on:
  issues:
    types: [opened]

jobs:
  triage:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Check account age
        id: check
        uses: actions/github-script@v7
        with:
          script: |
            const user = await github.rest.users.getByUsername({
              username: context.payload.issue.user.login
            });
            const created = new Date(user.data.created_at);
            const days = (Date.now() - created) / (1000 * 60 * 60 * 24);
            return days >= 30;
          result-encoding: string

      - uses: actions/checkout@v6
        if: steps.check.outputs.result == 'true'
        with:
          persist-credentials: false

      - uses: Lin-A1/newhorse/github@latest
        if: steps.check.outputs.result == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        with:
          model: anthropic/claude-sonnet-4-20250514
          prompt: |
            Review this issue. If there's a clear fix or relevant docs:
            - Provide documentation links
            - Add error handling guidance for code examples
            Otherwise, do not comment.
```

Za `issues` događaje, `prompt` unos je **potreban** jer nema komentara za izvlačenje instrukcija.

---

## Prilagođeni upiti

Zaobiđite zadani prompt da biste prilagodili ponašanje Newhorse za vaš tok posla.

```yaml title=".github/workflows/newhorse.yml"
- uses: Lin-A1/newhorse/github@latest
  with:
    model: anthropic/claude-sonnet-4-5
    prompt: |
      Review this pull request:
      - Check for code quality issues
      - Look for potential bugs
      - Suggest improvements
```

Ovo je korisno za provođenje specifičnih kriterija pregleda, standarda kodiranja ili fokusnih područja relevantnih za vaš projekt.

---

## Primjeri

Evo nekoliko primjera kako možete koristiti Newhorse u GitHub.

- **Objasnite problem**
  Dodajte ovaj komentar u GitHub izdanje.

```
  /newhorse explain this issue
```

Newhorse će pročitati cijelu temu, uključujući sve komentare, i odgovoriti s jasnim objašnjenjem.

- **Popravi problem**
  U izdanju na GitHub-u recite:

```
  /newhorse fix this
```

I Newhorse će kreirati novu granu, implementirati promjene i otvoriti PR sa promjenama.

- **Pregledajte PR-ove i izvršite izmjene**
  Ostavite sljedeći komentar na GitHub PR-u.

```
  Delete the attachment from S3 when the note is removed /oc
```

Newhorse će implementirati traženu promjenu i posvetiti je istom PR-u.

- **Pregledajte određene linije koda**
  Ostavite komentar direktno na linije koda u PR kartici "Files". Newhorse automatski detektuje datoteku, brojeve redova i kontekst razlike kako bi pružio precizne odgovore.

```
  [Comment on specific lines in Files tab]
  /oc add error handling here
```

Kada komentarišete određene linije, Newhorse prima:

- Tačan fajl se pregleda
- Specifične linije koda
- Okolni diff kontekst
- Informacije o broju linije
  Ovo omogućava više ciljanih zahtjeva bez potrebe za ručno specificiranjem putanja datoteka ili brojeva linija.
